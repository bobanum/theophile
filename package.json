{
  "name": "theophile",
  "version": "0.1.0",
  "description": "th2 - vanilla JS project using node modules (bundled with esbuild)",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "unpkg": "dist/bundle.standalone.min.js",
  "jsdelivr": "dist/bundle.standalone.min.js",
  "exports": {
    ".": {
      "browser": "./dist/bundle.standalone.min.js",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": true,
  "scripts": {
    "dev": "esbuild src/main.js --bundle --servedir=examples --outfile=examples/bundle.js --sourcemap --loader:.css=text",
    "build": "npm run build:rollup && npm run build:esbuild",
    "build:rollup": "rollup -c",
    "build:esbuild": "esbuild src/main.js --bundle --minify --outfile=examples/bundle.js --loader:.css=text",
    "watch": "rollup -c -w",
    "prepublishOnly": "npm run build:rollup",
    "publish:gh-pages": "npm run build:esbuild && npx gh-pages -d examples"
  },
  "dependencies": {
    "@bobanum/webponents": "github:bobanum/webponents",
    "dompurify": "^3.3.1",
    "highlight.js": "^11.11.1",
    "lodash": "^4.17.21",
    "marked": "^17.0.1"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^25.0.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "@rollup/plugin-terser": "^0.4.0",
    "esbuild": "^0.27.2",
    "gh-pages": "^5.0.0",
    "rollup": "^4.0.0",
    "rollup-plugin-postcss": "^4.0.2"
  }
}
